Two things to add to the table:

1. Add `totalIncome` for the year to the table. 
   1. Header: Income
   2. Where: in between `Household Member Age` and `Change`
2. Add `expenses` for the year to the table
   1. Header: Expenses
   2. Where: to the right of "Income"
